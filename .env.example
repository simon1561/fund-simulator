# Copy this file to .env and replace the token with your own copied Base token.
# You can paste either the full Base URL or only the token in configure_account.command.
BASE_TOKEN=replace_with_your_base_token
